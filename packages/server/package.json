{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server_start": "ts-node index.ts",
    "server_dev": "nodemon --watch '**/*.ts' --ignore '**/*.(spec|test).ts' --exec 'ts-node' index.ts",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.4.0"
  }
}
